import { CustomValueAccessorDirective } from './custom-value-accessor.directive';

describe('CustomValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
